# Minimal Supabase config for Edge Functions.
# Link the project with: npx supabase link --project-ref YOUR_PROJECT_REF

[functions.estimate-meal]
verify_jwt = true

# Shortcut uses a personal sync token (x-health-sync-token), not a user JWT.
[functions.sync-health]
verify_jwt = false
